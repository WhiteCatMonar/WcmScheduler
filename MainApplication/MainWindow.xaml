<Window x:Class="MainApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MainApplication"
        xmlns:views_ne_tab="clr-namespace:MainApplication.Views.NodeEditorTab"
        xmlns:converters="clr-namespace:MainApplication.ViewModels.Converters"
        mc:Ignorable="d"
        Title="WcmScheduler" Height="720" Width="1280">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    <DockPanel>
        <!-- メニューバー -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_ファイル">
                <MenuItem Header="読み込み" Command="{Binding LoadCommand}" />
                <MenuItem Header="保存" Command="{Binding SaveCommand}" />
                <MenuItem Header="名前を付けて保存" Command="{Binding SaveAsCommand}" />
            </MenuItem>
        </Menu>
        <!-- タブウィンドウ -->
        <TabControl ItemsSource="{Binding Tabs}"
                    SelectedItem="{Binding SelectedTab}">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ModelName}" />
                </DataTemplate>
            </TabControl.ItemTemplate>

            <TabControl.ContentTemplate>
                <DataTemplate>
                    <views_ne_tab:NodeEditorTab
                        Visibility="{Binding DataContext.IsNodeEditor,
                                     RelativeSource={RelativeSource AncestorType=Window}, 
                                     Converter={StaticResource BoolToVisibilityConverter}
                        }"/>
                </DataTemplate>
            </TabControl.ContentTemplate>
        </TabControl>
    </DockPanel>
</Window>
