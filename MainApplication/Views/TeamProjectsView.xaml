<UserControl x:Class="MainApplication.Views.TeamProjectsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{DynamicResource WindowBackgroundBrush}"
          TextElement.Foreground="{DynamicResource WindowTextBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="4">
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="追加"
                        Command="{Binding AddProjectCommand}"
                        Width="64"
                        Margin="0,0,4,0"/>
                <Button Content="削除"
                        Command="{Binding DeleteSelectedProjectCommand}"
                        IsEnabled="{Binding CanDeleteSelectedProject}"
                        Width="64"/>
            </StackPanel>
        </DockPanel>

        <TabControl Grid.Row="1"
                    ItemsSource="{Binding Tabs}"
                    SelectedItem="{Binding SelectedTab}"
                    Style="{StaticResource ThemedTabControlStyle}"
                    ItemContainerStyle="{StaticResource ThemedTabItemStyle}">

            <!-- タブ名 -->
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Content.ProjectName}" />
                </DataTemplate>
            </TabControl.ItemTemplate>

            <!-- タブ内容 -->
            <TabControl.ContentTemplate>
                <DataTemplate>
                    <ContentPresenter Content="{Binding Content}" />
                </DataTemplate>
            </TabControl.ContentTemplate>

        </TabControl>
    </Grid>
</UserControl>
