<Application x:Class="MainApplication.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MainApplication.ViewModels"
             xmlns:vm_project="clr-namespace:MainApplication.ViewModels.ProjectModel"
             xmlns:vm_team="clr-namespace:MainApplication.ViewModels.TeamModel"
             xmlns:views="clr-namespace:MainApplication.Views"
             xmlns:views_ne_tab="clr-namespace:MainApplication.Views.NodeEditorTab"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style x:Key="ThemedTabControlStyle" TargetType="TabControl">
            <Setter Property="Background" Value="{DynamicResource TabWindowBackgroundBrush}"/>
        </Style>

        <Style x:Key="ThemedTabItemStyle" TargetType="TabItem">
            <Setter Property="Background" Value="{DynamicResource TabItemBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TabItemTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource NodeEditorBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter x:Name="ContentSite"
                                              ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="{DynamicResource TabItemSelectedBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TabItemSelectedTextBrush}"/>
                                <Setter Property="Panel.ZIndex" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource TabItemSelectedBackgroundBrush}"/>
                    <Setter Property="Foreground" Value="{DynamicResource TabItemSelectedTextBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- TeamProjectsViewModel → TeamProjectsView -->
        <DataTemplate DataType="{x:Type vm:TeamProjectsViewModel}">
            <views:TeamProjectsView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm_team:TeamSettingsViewModel}">
            <views:TeamSettingsView />
        </DataTemplate>
        
        <!-- ProjectViewModel → ProjectView -->
        <DataTemplate DataType="{x:Type vm_project:ProjectViewModel}">
            <views:ProjectView />
        </DataTemplate>

        <!-- NodeEditorViewModel → NodeEditorTab -->
        <DataTemplate DataType="{x:Type vm_project:NodeEditorViewModel}">
            <views_ne_tab:NodeEditorTab />
        </DataTemplate>
        
    </Application.Resources>
</Application>
